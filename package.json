{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp test"
  },
  "devDependencies": {
    "pulp": "^8.1.1",
    "rimraf": "^2.5.0"
  }
}
